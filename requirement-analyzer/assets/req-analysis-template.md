# 需求分析文档模板（8 章节）

> 这是分析子 Agent 的输出格式模板。每个 `01-<slug>.md` 文件必须严格遵循此结构。
> **双证据原则**：第 4 节的每个"已完成"声明必须同时提供 commit hash 和文件:行号。缺少任一证据，判定为 NO 或 PARTIAL。

---

# 需求：<需求标题>

> **分析时间**：<YYYY-MM-DD HH:MM>
> **分析分支**：<当前分支名>
> **需求状态**：<飞书 story 状态> / <优先级>
> **产品线**：<产品线名称>
> **代码仓库**：<Git 仓库路径>
> **结论先行**：**已完成（YES）/ 未完成（NO）/ 部分完成（PARTIAL）**

---

## 1. 需求摘要

| 维度 | 内容 |
|------|------|
| 背景 | <需求产生的业务背景，从 wiki/description 提炼> |
| 目标 | <需求要达成的具体目标> |
| 范围 | <明确划定的修改范围> |
| 非目标 | <明确排除在本次需求之外的内容> |

---

## 2. 代码现状定位（关键锚点）

> 用锚点编号（① ② ③）标注关键代码位置。每个锚点包含：文件路径、行号范围、代码片段、说明。

### 锚点 ①：<锚点名称>

`<相对路径（从 src/main/java/ 开始）>` **lines <起始行>-<结束行>**

```java
// <关键代码片段，包含足够上下文>
```

- **说明**：<这个锚点为什么关键，它与需求的关联>
- **调用上下文**：<在哪些场景下会被触发>

### 锚点 ②：<锚点名称>

（同上格式，至少 2 个锚点）

### 关键缺失（如适用）

| 文件 | 预期内容 | 当前状态 |
|------|---------|---------|
| `<路径>` | <应该存在但尚未实现的功能> | 不存在 / 未实现 |

---

## 3. 关键链路追踪

> 用 ASCII 树状图展示核心调用链，从 API 入口到数据持久化或外部调用。

```
<Controller.method>                        [Controller, line N]
  └─ <Service.method(params)>              [Service, line N]
       ├─ dao.<操作>                       [DAO]
       ├─ <辅助方法>(...)                  [Service, line N]
       └─ CompletableFuture.runAsync →     [Async]
            ├─ feignConnector.call()       [Feign → 外部]
            └─ noticeService.send()        [Service, line N]
```

### 链路影响面

- **直接修改点**：<列出需要修改的具体文件和方法>
- **间接影响**：<列出间接受影响的模块、缓存、消息队列等>
- **关联接口**：`POST /hxl.fsms.<domain>.<action>`

---

## 4. 已完成判定

### 4.1 判定结论：**YES / NO / PARTIAL**

### 4.2 双证据

#### 已完成项（YES 时填写）

| 证据维度 | 期望 | 实际 |
|----------|------|------|
| 1. 提交记录 | <期望的 commit 信息> | <实际的 commit hash + title> |
| 2. 关键代码段 | <期望的代码行为> | `<文件路径:行号>`：实际代码片段 |

#### 未完成项（NO/PARTIAL 时填写）

| 差距项 | 当前状态 | 证据 |
|--------|---------|------|
| <差距描述> | <现状> | `git log -G "<关键词>"` 0 命中 / `<文件:行号>` 仍用旧逻辑 |

### 4.3 验证项 Checklist

| 验证项 | 状态 | 证据 |
|--------|------|------|
| <具体验证点 1> | ✅ / ❌ / 🟡 | `<文件路径:行号>` 或 commit hash |
| <具体验证点 2> | ✅ / ❌ / 🟡 | ... |

---

## 5. 修改建议

### 5.1 改动文件清单

| # | 文件 | 改动 | 类型 |
|---|------|------|------|
| ① | `<相对路径>` | <具体改动描述，包含方法签名或关键逻辑> | 新增 / 修改 / 删除 |
| ② | `<相对路径>`（**新建**） | <新文件用途和核心内容> | 新增 |

### 5.2 关键代码骨架

```java
// <文件路径>
// <方法/类名>（新增或修改）
<代码骨架，标注关键逻辑位置>
```

### 5.3 可复用的现有机制

- <项目中已有的工具类、枚举、Service 方法等可直接复用>
- <类似需求的参考实现路径>

### 5.4 DDL（如涉及数据库变更）

```sql
-- <说明>
ALTER TABLE <table_name> ADD COLUMN / MODIFY COLUMN <column_def> COMMENT '<注释>';
```

### 5.5 灰度 / 迁移建议

1. **首期灰度**（<时间估算>）：<具体灰度策略>
2. **扩容**：<逐企业/逐环境推广方案>
3. **全量上线**：<上线检查点>
4. **回退预案**：<回退操作步骤>

---

## 6. 风险与边界

### 6.1 风险点

| 风险 | 影响 | 缓解 |
|------|------|------|
| <风险描述> | <如果不处理会导致什么后果> | <降低风险的具体措施> |

### 6.2 边界项

- <边界条件 1>：<说明>
- <边界条件 2>：<说明>

### 6.3 已知遗留

- <当前已知但本次不处理的遗留问题>
- <后续迭代中需要关注的改进点>

---

## 7. 关联引用

### 7.1 Wiki / 需求文档

- <飞书 wiki URL 或文档路径>

### 7.2 源码（绝对路径）

- `<文件路径>:<行号范围>` — <说明>

### 7.3 Commit

| Hash | 时间 | 作者 | 说明 |
|------|------|------|------|
| `<完整 hash>` | `<YYYY-MM-DD>` | `<作者>` | <说明> |

### 7.4 关联接口

- `POST /hxl.fsms.<domain>.<action>` — <说明>
- Feign：`<connector.method>` — <说明>

### 7.5 知识图谱节点

- `<codebase-memory search_graph 查询到的关键函数/类>`

---

## 8. 结论

- **状态判定**：**YES / NO / PARTIAL**
- **核心改造点**：<一句话概括最关键的修改>
- **最简里程碑**：<完成此需求的最小可行步骤，1-3 条>
- **阻塞项**：<当前阻止开发推进的依赖或待确认事项>
- **预估工作量**：<用天或小时表达，如"1-2 天">

---

<!-- 以下区域由审核 Agent 填写，分析 Agent 不修改 -->

## 修改日志

| 时间 | 改动 | 原因 |
|------|------|------|
| | | |
